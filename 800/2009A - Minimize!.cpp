#include "bits/stdc++.h"
using namespace std;

void solve() {
    int n, a, b;
    cin >> n;

    while(n--) {
        cin >> a >> b;
        cout<<b-a<<"\n";
    }


}

int main() {
    cin.tie(0);
    ios::sync_with_stdio(false);
    solve();
    return 0;
}